import React from 'react';
import StaggerChilder from "@/app/animations/staggerChilder";

const Page = () => {
    return (
        <div>
            <StaggerChilder />
        </div>
    );
};

export default Page;